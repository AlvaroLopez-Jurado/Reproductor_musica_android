<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alvaro.prac2">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListasAdapter" />
        <activity android:name=".CancionesAdapter" />
        <activity android:name=".HomeFragment" />
        <activity
            android:name=".Listas"
            android:label="@string/title_activity_listas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GridFragment" />
        <activity
            android:name=".AddList"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Settings" />
        <activity android:name=".MenuLista"></activity>
        <activity android:name=".MenuGrid"></activity>
        <activity android:name=".PlaySong"
            android:theme="@style/AppTheme.CustomTheme"></activity>
    </application>

</manifest>